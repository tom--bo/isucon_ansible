    - name: update apt
      apt: update_cache=yes
      when: ansible_distribution == "Ubuntu"

    - name: import apt repositories
      apt: name={{ item }} state=installed
      when: ansible_distribution == "Ubuntu"
      with_items:
        - vim
        - htop
        - tmux
        - zsh
        - git

