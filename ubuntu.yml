    - name: update apt & install
      apt: name={{item}} state=present update_cache=yes
      when: ansible_distribution == "Ubuntu"
      with_items:
        - vim
        - htop
        - tmux
        - zsh
        - git

